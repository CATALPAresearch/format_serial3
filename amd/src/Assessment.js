define(['jquery'], function ($) {

    var Assessment = function (Vue, d3, dc, crossfilter, moment) {
        var width = document.getElementById('planing-component').offsetWidth;
        var margins = { top: 15, right: 10, bottom: 20, left: 10 };

        // Sofern in der renderer.php ein <div> mit einer ID versehen ist, kannst du es hier z.B. mit jQuery adressierenbundleRenderer.renderToStream
        // bspw. $('#dieIDmeinesElements')
        

    };

    return Assessment;
});
